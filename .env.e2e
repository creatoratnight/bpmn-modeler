# Loaded by Vite when started with `--mode e2e` (npm run dev:e2e).
# Switches the Firebase SDK to the local emulators and enables the
# Playwright sign-in hook in src/config/.firebase.js.
VITE_FIREBASE_EMULATOR=true
